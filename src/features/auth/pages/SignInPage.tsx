import { SignInForm } from "../components";

const SignInPage = () => {
  return (
    <section className="size-screen flex-center">
      <SignInForm />
    </section>
  );
};

export default SignInPage;
